 {
  "name": "carithm-seo-engine",
  "version": "1.0.0",
  "description": "Programmatic SEO generator for car repair cost pages",
  "main": "cars/generate-pages.js",
  "scripts": {
    "generate": "node cars/generate-pages.js",
    "generate-audio": "python3 audio/src/cardiag/web/static/generate_pages.py",
    "sitemap": "node generate-sitemap.js",
    "build": "npm run generate-audio && npm run generate && npm run sitemap",
    "deploy": "npm run build"
  },
  "dependencies": {
    "fs-extra": "^11.1.1",
    "handlebars": "^4.7.8"
  }
}
